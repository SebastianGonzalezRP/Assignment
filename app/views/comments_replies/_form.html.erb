<%= form_with(model: comments_reply, local: true) do |form| %>
  <% if comments_reply.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comments_reply.errors.count, "error") %> prohibited this comments_reply from being saved:</h2>

      <ul>
        <% comments_reply.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
