<p id="notice"><%= notice %></p>

<div class="row ml-0 mt-3" id="div-principal-text">
  <div>
    <h1>Organizations</h1>
    <p>In this section we can find all the organizations there are, we can see the title along with its description.
      Pressing each organization leads us to the organization page where we can see the photos and a greater amount of information
    </p>
    <p>
      We can create a organization with the add button
    </p>
  </div>
  <div class="d-flex justify-content-between align-items-center">
    <div class="btn-group">
      <button class="btn btn-primary" id="add_button_event"><%=link_to "Add", new_organization_path %></button>
    </div>
  </div>
</div>

<div class="row">
  <% @organizations.each do |organization| %>
    <div class="col-md-4" id="center-div-home">
      <div class="card mb-4 shadow-sm">
        <%= image_tag("thumbnail.svg", class: "card-img-top", width: "100%", background: "#55595c", color: "#eceeef") %>
        <div class="card-body">
          <p class="card-text"><%= link_to organization.name, organization %></p>
          <p class="card-text" id="text-div-black"><%= organization.description %></p>
          <div class="btn-group">
            <button type="button" class="btn btn-sm btn-outline-info"><%= link_to "View", organization %></button>
            <button type="button" class="btn btn-sm btn-outline-warning"><%= link_to "Edit",  edit_organization_path(organization) %></button>
          </div>
        </div>
      </div>
    </div>
  <% end %>
</div>
